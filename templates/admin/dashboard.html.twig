
{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <div class="text-center">
        <img src="{{ asset('../images/logoEasyAdmin.png')}}" alt="logo">
    
        {% if app.user %}
            <div>
               <h3>Bienvenue {{ app.user.nom}}  dans votre espace administrateur.</h3> 
            </div>
        {% endif %} 
            <div>
                <button type="button" class="btn btn-success mt-5"><a  href="{{ path('app_logout') }}"></a>Se deconnecter</button>
                
            </div>
    </div>
{% endblock %}    

