{% extends 'base.html.twig' %}

{% block title %} PortFolio enfant - Photomaeght {% endblock %}

{% block body %}
    <main>
        <div class="retour">
            <a href="{{ path('app_accueil') }}#categoriePhotos"><p>Retour catégories portfolio</p></a>
        </div>
        <div class="galerie">
        
            {% for enfant  in photosEnfantPortfolio %}
                <div class="containerImg">
                    <img src=" {{ vich_uploader_asset(enfant, 'imageFile') | imagine_filter('portfolio') }}" alt="photo_enfant" >
                </div>
            {% endfor %}
        </div>
        <div class="containerPagination">
            {% do photosEnfantPortfolio.setPageRange(2) %}
            {{ knp_pagination_render(photosEnfantPortfolio, 'pagination/pagination.html.twig') }}
        </div>
    </main>
{% endblock %}
